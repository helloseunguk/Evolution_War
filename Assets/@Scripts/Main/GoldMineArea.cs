using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoldMineArea : MonoBehaviour
{
    public List<GoldMinePlatform> unitPlatforms = new List<GoldMinePlatform>();

    // Start is called before the first frame update
    void Start()
    {
    }
}
